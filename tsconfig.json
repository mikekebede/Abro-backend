{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2020",                /* Modern JavaScript features for Node.js. */
    "typeRoots": ["./src/types", "./node_modules/@types"],
    "module": "commonjs",              /* Node.js uses CommonJS module resolution. */
    "rootDir": "./src",                /* Source files folder. */
    "outDir": "./dist",                /* Compiled JavaScript output folder. */
    "strict": true,                    /* Enable strict type checking. */
    "esModuleInterop": true,           /* Support default imports from CommonJS modules. */
    "forceConsistentCasingInFileNames": true, /* Enforce case sensitivity in imports. */
    "skipLibCheck": true,              /* Skip type-checking .d.ts files for faster builds. */

    /* Module Resolution */
    "moduleResolution": "node",       /* Use Node.js module resolution algorithm. */
    "resolveJsonModule": true,        /* Allow importing JSON files. */
    "allowSyntheticDefaultImports": true, /* Allow default imports even if not explicitly defined. */

    /* Type Checking */
    "noImplicitAny": true,            /* Catch variables and parameters with implicit 'any' type. */
    "strictNullChecks": true,         /* Ensure proper handling of `null` and `undefined`. */
    "noImplicitThis": true,           /* Catch 'this' being implicitly typed as 'any'. */
    "alwaysStrict": true,             /* Emit 'use strict' in JavaScript files. */

    /* Output Options */
    "sourceMap": true,                /* Emit source maps for debugging. */

    /* Paths */
    "baseUrl": "./",                  /* Base directory for resolving non-relative paths. */
    "paths": {                        /* Optional: Alias imports for cleaner code. */
      "@models/*": ["src/models/*"],
      "@controllers/*": ["src/controllers/*"],
      "@middlewares/*": ["src/middlewares/*"],
      "@config/*": ["src/config/*"]
    }
  },
  "include": ["src/**/*"],            /* Include all files in the src folder. */
  "exclude": ["node_modules", "dist"] /* Exclude these folders from the build process. */
}
